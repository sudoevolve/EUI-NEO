#include "core/render/vulkan/vulkan_backend.h"

namespace core::render::vulkan {

// Text rendering is implemented on VulkanRenderBackend so the shared
// TextPrimitive CPU layer can feed the same draw command to all backends.

} // namespace core::render::vulkan
